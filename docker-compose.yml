services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - ./seed-db-scripts/postgres/001_seed_users.sql:/docker-entrypoint-initdb.d/init.sql

  # mongo:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
