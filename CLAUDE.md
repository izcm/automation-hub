# Notes for Claude

- The user owns all `@a2zb/*` packages (e.g. `@a2zb/react`, `@a2zb/lib`). When a fix or improvement would belong in one of those packages rather than in this repo, suggest that instead of working around it here.
